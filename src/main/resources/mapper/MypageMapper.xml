<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.road_friends.everycar.mypage.mapper.MypageMapper">

    <!-- 로그인한 사용자 정보 조회 -->
    <select id="getUserInfo" resultType="com.road_friends.everycar.mypage.dto.MypageDTO">
        SELECT user_num, user_id, user_name, user_email,
        user_phone, user_gender, user_birth, user_address,
        user_status, enabled
        FROM user WHERE user_id = #{userId}
    </select>


</mapper>
